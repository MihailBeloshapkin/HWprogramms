#pragma once

// Get stting from console.
void getString(char *str, int maxSize);

// Copy string and retorn new string pointer.
char *copy(char *string);

// Check if symbol is letter.
bool isLetter(char symbol);